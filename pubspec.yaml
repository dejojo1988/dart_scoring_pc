DATEI: pubspec.yaml

name: dart_scoring_pc
description: "Dart Scoring Software for Windows PC."
publish_to: 'none'

version: 1.0.0+3

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  sqflite_common_ffi: ^2.3.4+4
  path: ^1.9.0
  audioplayers: ^6.6.0
  file_picker: ^8.0.7
  window_manager: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/audio/default_pack/